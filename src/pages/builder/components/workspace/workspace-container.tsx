import { Box, styled } from '@mui/material';

export const WorkspaceContainer = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
  flexGrow: 1,
  height: '100%',
  width: '100%',
}));
